services:
  - type: web
    name: api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    pythonVersion: 3.11  # ← FIX: Python 3.11 (pandas compila perfeito)
